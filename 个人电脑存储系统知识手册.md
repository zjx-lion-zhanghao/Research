# 个人电脑存储系统知识手册

> 适用设备与场景：Windows 笔记本（华硕天选 5 Pro）、Apple Silicon MacBook Air、外置 NVMe SSD、移动机械硬盘、Ubuntu 外置或内置安装、ROS 2 / PX4 / Docker / Gazebo 开发。
>
> 文档目标：建立一套从**物理介质、接口协议、分区文件系统，到系统启动、故障与备份**的完整存储知识框架。

---

## 目录

1. [存储系统的整体地图](#1-存储系统的整体地图)
2. [内存与硬盘的区别](#2-内存与硬盘的区别)
3. [机械硬盘 HDD](#3-机械硬盘-hdd)
4. [固态硬盘 SSD](#4-固态硬盘-ssd)
5. [SATA、PCIe、NVMe、M.2 的关系](#5-satapcienvmem2-的关系)
6. [USB、USB-C、USB4 与 Thunderbolt](#6-usbusb-cusb4-与-thunderbolt)
7. [NVMe 硬盘盒](#7-nvme-硬盘盒)
8. [性能指标：顺序、随机、IOPS 与延迟](#8-性能指标顺序随机iops-与延迟)
9. [物理磁盘、分区、卷与文件系统](#9-物理磁盘分区卷与文件系统)
10. [GPT、MBR 与 UEFI](#10-gptmbr-与-uefi)
11. [NTFS、exFAT、ext4、APFS 与 FAT32](#11-ntfsexfatext4apfs-与-fat32)
12. [Ubuntu 安装盘、启动盘和系统盘](#12-ubuntu-安装盘启动盘和系统盘)
13. [外置 NVMe 安装 Ubuntu](#13-外置-nvme-安装-ubuntu)
14. [硬盘为什么会损坏](#14-硬盘为什么会损坏)
15. [SMART、检测和验盘](#15-smart检测和验盘)
16. [删除、格式化与安全擦除](#16-删除格式化与安全擦除)
17. [安全弹出、断电与文件系统损坏](#17-安全弹出断电与文件系统损坏)
18. [备份、同步、归档与 RAID](#18-备份同步归档与-raid)
19. [不同存储设备的正确用途](#19-不同存储设备的正确用途)
20. [适合你的存储结构](#20-适合你的存储结构)
21. [购买 NVMe SSD 的判断方法](#21-购买-nvme-ssd-的判断方法)
22. [512GB、1TB 和 2TB 如何选择](#22-512gb1tb-和-2tb-如何选择)
23. [常见误区](#23-常见误区)
24. [最终检查清单](#24-最终检查清单)

---

# 1. 存储系统的整体地图

一份文件从物理设备变成你在资源管理器或 Finder 中看到的文件，需要经过多层结构：

```text
物理存储介质
    ↓
设备控制器与固件
    ↓
存储通信协议
    ↓
总线和物理接口
    ↓
分区表
    ↓
分区 / 卷
    ↓
文件系统
    ↓
文件夹与文件
```

以“NVMe SSD + USB-C 硬盘盒 + Ubuntu”为例：

```text
NAND 闪存
    ↓
SSD 主控与固件
    ↓
NVMe 协议
    ↓
M.2 2280 接口
    ↓
硬盘盒桥接芯片
    ↓
USB 3.2 Gen 2
    ↓
USB-C 数据线
    ↓
电脑 USB-C 接口
    ↓
GPT 分区表
    ↓
FAT32 EFI 分区 + ext4 系统分区
    ↓
Ubuntu 系统文件
```

这些名词不在同一个层级：

| 层级 | 常见概念 | 解决的问题 |
|---|---|---|
| 存储介质 | HDD、NAND 闪存 | 数据实际存在哪里 |
| 设备协议 | SATA、NVMe | 主机怎样向硬盘发命令 |
| 传输总线 | PCIe、USB | 数据经过哪条通道 |
| 外形规格 | 2.5 英寸、3.5 英寸、M.2 2280 | 设备长什么样、如何安装 |
| 插头形状 | USB-A、USB-C | 物理接口是什么形状 |
| 分区表 | GPT、MBR | 一块盘怎样划分区域 |
| 文件系统 | NTFS、exFAT、ext4、APFS | 文件怎样组织和管理 |
| 启动组件 | UEFI、EFI、GRUB | 电脑怎样启动操作系统 |

以后分析任何硬盘，都可以逐层判断，而不是只看商品标题中的“高速”“Type-C”或“NVMe”。

---

# 2. 内存与硬盘的区别

中文日常交流中，经常把内存和硬盘空间混在一起。

## 2.1 内存 RAM

内存保存当前正在运行的数据，例如：

- 浏览器页面；
- 正在运行的程序；
- Gazebo 场景；
- ROS 2 节点状态；
- Docker 容器正在使用的数据；
- 视频剪辑的临时素材。

特点：

- 速度非常快；
- 断电后数据消失；
- 常见容量为 16GB、32GB、64GB。

## 2.2 HDD 和 SSD

硬盘或固态硬盘负责长期保存：

- Windows、Ubuntu、macOS；
- 软件；
- 项目代码；
- 视频；
- 数据集；
- 文档；
- 飞行日志。

特点：

- 断电后数据仍然存在；
- 常见容量为 512GB、1TB、2TB、4TB。

## 2.3 类比

```text
CPU       = 正在工作的人
RAM       = 办公桌
内置 SSD  = 身边的文件柜
移动 SSD  = 可随身携带的高速文件箱
机械硬盘  = 大容量仓库
云盘/NAS  = 远程或网络仓库
```

RAM 不足时，程序运行会卡；硬盘空间不足时，文件无法继续保存。

---

# 3. 机械硬盘 HDD

HDD 全称为 Hard Disk Drive。

内部主要有：

- 磁性盘片；
- 主轴电机；
- 读写磁头；
- 磁头执行机构；
- 控制电路；
- 缓存。

工作过程类似唱片机：

```text
盘片旋转
    ↓
磁头移动到目标磁道
    ↓
等待目标区域转到磁头下方
    ↓
读取或写入磁性数据
```

## 3.1 HDD 的优点

- 单位容量价格低；
- 容易获得 2TB、4TB、8TB 甚至更大容量；
- 大文件连续读写尚可；
- 适合视频、照片、数据集和历史项目；
- 适合作为冷备仓库。

普通 2.5 英寸移动机械硬盘的大文件速度一般约为：

```text
100～150 MB/s
```

## 3.2 HDD 的缺点

- 读取大量小文件很慢；
- 有机械结构，会磨损；
- 工作时怕碰撞、跌落和剧烈移动；
- 会有轻微噪声和震动；
- 不适合作为高负载系统盘；
- 不适合频繁运行 Docker、ROS 2 编译和虚拟机。

## 3.3 为什么 HDD 复制视频还行，运行系统却卡

复制一个 30GB 视频属于顺序读写，磁头可以较连续地工作。

启动 Ubuntu、安装软件或编译工程则会读取成千上万个分散的小文件，磁头必须反复移动。机械寻道延迟会严重限制系统体验。

## 3.4 2.5 英寸与 3.5 英寸

| 类型 | 典型用途 | 特点 |
|---|---|---|
| 2.5 英寸 HDD | 移动硬盘、旧笔记本 | 小巧，可由 USB 供电 |
| 3.5 英寸 HDD | 台式机、NAS、桌面存储 | 容量大，通常需要独立供电 |

## 3.5 CMR 与 SMR

### CMR

磁道相对独立。

优点：

- 持续写入较稳定；
- 频繁修改文件时表现较好；
- 更适合 NAS 和较重负载。

### SMR

磁道部分重叠，类似屋顶瓦片。

优点：

- 提高存储密度；
- 成本较低；
- 适合写入后很少修改的数据。

缺点：

- 大量连续写入时可能掉速；
- 频繁删除和覆盖时性能较差。

SMR 作为冷备盘通常可以接受，但不适合作为 Ubuntu、Docker 或编译工作盘。

---

# 4. 固态硬盘 SSD

SSD 全称为 Solid State Drive。

SSD 内部没有盘片和磁头，主要包含：

- NAND 闪存；
- SSD 主控；
- 固件；
- 缓存；
- 电源管理电路；
- 有些型号还包含独立 DRAM。

## 4.1 NAND 闪存

NAND 是真正保存数据的存储芯片。

SSD 主控负责：

- 地址映射；
- 错误纠正；
- 坏块管理；
- 磨损均衡；
- 垃圾回收；
- 缓存管理；
- 温度和功耗控制。

因此，SSD 可靠性不仅取决于闪存颗粒，也取决于主控和固件。

## 4.2 SSD 的优点

- 随机访问速度快；
- 系统启动和软件打开快；
- 没有机械噪声；
- 抗震性优于 HDD；
- 适合操作系统、编译、Docker 和虚拟机；
- 体积较小。

## 4.3 SSD 的缺点

- 每 TB 价格通常高于 HDD；
- 主控或固件故障时可能突然整盘无法识别；
- 闪存具有有限擦写寿命；
- 某些低端 SSD 持续写入会严重掉速；
- 数据恢复通常比 HDD 控制器故障更复杂。

## 4.4 SLC、MLC、TLC、QLC

| 类型 | 每个单元保存 | 一般特点 |
|---|---:|---|
| SLC | 1 bit | 快、耐用、昂贵 |
| MLC | 2 bit | 较耐用，消费级较少 |
| TLC | 3 bit | 性能、寿命和价格较均衡 |
| QLC | 4 bit | 容量便宜，持续写入较弱 |

用于 Ubuntu、ROS 2、Docker 和开发环境时，优先考虑 TLC。但一个正规品牌、固件成熟的 QLC SSD，仍可能比来源不明的杂牌 TLC 更可靠。

## 4.5 为什么 SSD 会先快后慢

许多 SSD 会把一部分 TLC 或 QLC 空间模拟成高速 SLC 缓存：

```text
开始写入
    ↓
先写入 SLC 缓存
    ↓
短时间速度很快
    ↓
缓存写满
    ↓
直接写入 TLC / QLC
    ↓
速度下降
```

因此商品宣传的“最高速度”通常不是整个硬盘全程持续速度。

## 4.6 TBW

TBW 表示厂商标称的累计写入耐久度。

例如 300TBW 表示设计和保修指标允许累计写入约 300TB，但它不代表 SSD 在到达 300TBW 前绝对不会损坏。

SSD 也可能因以下问题提前故障：

- 主控故障；
- 固件异常；
- 供电问题；
- 过热；
- USB 桥接故障；
- 电路损坏。

---

# 5. SATA、PCIe、NVMe、M.2 的关系

## 5.1 SATA

SATA 是较早的存储接口与协议体系，最初主要面向机械硬盘。

常见 SATA 设备：

- 2.5 英寸 SATA SSD；
- 2.5 英寸 HDD；
- 3.5 英寸 HDD；
- M.2 SATA SSD。

SATA SSD 的典型顺序速度约为：

```text
450～550 MB/s
```

## 5.2 PCIe

PCIe 是电脑内部高速总线。

显卡、NVMe SSD、网卡等设备都可以通过 PCIe 与系统通信。

例如：

```text
PCIe 4.0 ×4
```

表示使用 PCIe 第四代、四条通道。

## 5.3 NVMe

NVMe 是专门面向高速非易失性存储设计的通信协议，消费级 NVMe SSD 通常通过 PCIe 连接。

可以这样类比：

```text
PCIe = 高速公路
NVMe = 交通规则
SSD  = 运送数据的车辆
```

## 5.4 M.2

M.2 主要是设备外形和接口规格，不代表一定是 NVMe。

M.2 设备可能是：

- NVMe SSD；
- SATA SSD；
- Wi-Fi 网卡；
- 蜂窝通信模块。

因此：

```text
M.2 ≠ NVMe
```

购买硬盘盒或 SSD 时必须确认协议。

## 5.5 M.2 2280

M.2 2280 表示：

- 宽 22mm；
- 长 80mm。

常见尺寸：

| 规格 | 尺寸 |
|---|---|
| 2230 | 22 × 30mm |
| 2242 | 22 × 42mm |
| 2260 | 22 × 60mm |
| 2280 | 22 × 80mm |

普通笔记本 NVMe SSD 最常见的是 2280。

---

# 6. USB、USB-C、USB4 与 Thunderbolt

## 6.1 USB-A 与 USB-C

USB-A、USB-C 主要描述插头形状。

```text
USB-C ≠ 一定高速
```

一个 USB-C 接口可能只支持：

- USB 2.0；
- USB 5Gbps；
- USB 10Gbps；
- USB 20Gbps；
- USB4；
- Thunderbolt；
- 显示输出；
- 充电。

## 6.2 常见外置速率

| 标称链路 | 理论速率 | 常见实际文件速度 |
|---|---:|---:|
| USB 5Gbps | 5Gbps | 约 350～500MB/s |
| USB 10Gbps | 10Gbps | 约 750～1050MB/s |
| USB 20Gbps | 20Gbps | 约 1400～2000MB/s |
| USB4 / Thunderbolt 40Gbps | 40Gbps | 约 2500～3800MB/s |

实际速度还取决于：

- SSD；
- 硬盘盒；
- 桥接芯片；
- 数据线；
- 电脑接口；
- 温度；
- 文件大小；
- 源盘速度。

---

# 7. NVMe 硬盘盒

NVMe 硬盘盒不是单纯的保护壳。

它通常包含：

- M.2 NVMe 插槽；
- NVMe 转 USB 桥接芯片；
- 供电电路；
- USB-C 接口；
- 导热垫；
- 铝合金外壳；
- 固件。

完整链路：

```text
NVMe SSD
    ↓
硬盘盒桥接芯片
    ↓
USB 3.2 / USB4
    ↓
数据线
    ↓
电脑
```

## 7.1 硬盘盒影响什么

- 速度；
- 温度；
- 是否掉盘；
- 睡眠唤醒稳定性；
- 是否支持 UASP；
- 是否透传 SMART；
- 是否支持 TRIM；
- 是否适合长期运行外置系统。

## 7.2 10Gbps 硬盘盒的意义

10Gbps 理论上约为：

```text
10Gbps ÷ 8 = 1.25GB/s
```

扣除协议和桥接开销后，NVMe 外置盘实际约 800～1050MB/s 属于正常范围。

因此即使 SSD 本身标称 7000MB/s，装进 10Gbps 硬盘盒后也只能发挥约 1GB/s。

实际速度由最慢环节决定：

```text
实际速度 =
min(
  SSD 性能,
  硬盘盒性能,
  USB 接口性能,
  数据线性能,
  源盘性能
)
```

## 7.3 选硬盘盒时看什么

- 明确支持 M.2 NVMe；
- 支持 2280；
- 不是仅支持 M.2 SATA；
- 铝合金外壳；
- 有完整导热垫；
- 使用成熟桥接芯片；
- 长时间传输不掉盘；
- 睡眠唤醒稳定；
- 支持 UASP；
- 数据线牢固。

用于 Ubuntu 系统盘时：

```text
稳定性 > 峰值跑分
```

---

# 8. 性能指标：顺序、随机、IOPS 与延迟

## 8.1 顺序读写

连续处理大文件，例如：

- 视频；
- 镜像；
- 压缩包；
- 大型数据文件。

单位一般为 MB/s 或 GB/s。

## 8.2 随机读写

处理大量分散的小文件，例如：

- 启动系统；
- 打开软件；
- Docker；
- Python 虚拟环境；
- Git 仓库；
- ROS 2 编译；
- `apt install`。

## 8.3 IOPS

IOPS 表示每秒可以完成多少次输入输出操作，通常用于描述随机读写能力。

## 8.4 延迟

延迟表示发出请求到数据返回所需的时间。

| 设备 | 延迟特点 |
|---|---|
| HDD | 毫秒级 |
| SATA SSD | 微秒级 |
| NVMe SSD | 更低的微秒级 |
| RAM | 纳秒级 |

系统体验通常更受随机性能和延迟影响，而不是只看最高顺序速度。

---

# 9. 物理磁盘、分区、卷与文件系统

假设电脑中有一块 1TB SSD。

## 9.1 物理磁盘

这是真实存在的一块硬件设备。

## 9.2 分区表

分区表描述整块盘如何划分。

常见类型：

- GPT；
- MBR。

## 9.3 分区

一块 1TB SSD 可以划分为：

- EFI 分区；
- Windows 分区；
- Ubuntu 分区；
- 数据共享分区。

## 9.4 卷

卷是操作系统能够挂载并使用的逻辑存储区域。

Windows 中的：

```text
C:
D:
E:
```

通常是卷的盘符，不一定代表三块物理硬盘。

一块物理 SSD 完全可以划分成 C 盘和 D 盘。

## 9.5 文件系统

文件系统决定文件如何组织，包括：

- 文件名；
- 文件夹；
- 权限；
- 日志；
- 符号链接；
- 空间分配；
- 错误恢复。

---

# 10. GPT、MBR 与 UEFI

## 10.1 MBR

较老的分区表方式。

特点：

- 面向传统 BIOS；
- 大容量支持有限；
- 分区数量限制较多。

## 10.2 GPT

现代分区表方式。

特点：

- 适合 UEFI；
- 支持大容量硬盘；
- 支持更多分区；
- 保存分区表备份；
- 现代 Windows、Linux 和 macOS 更适合使用。

现代电脑通常使用：

```text
UEFI + GPT
```

## 10.3 UEFI

UEFI 是现代电脑的固件启动机制。

Ubuntu 启动大致过程：

```text
开机
  ↓
UEFI
  ↓
EFI 系统分区
  ↓
GRUB
  ↓
Linux 内核
  ↓
Ubuntu
```

---

# 11. NTFS、exFAT、ext4、APFS 与 FAT32

## 11.1 NTFS

Windows 的主要文件系统。

适合：

- Windows 系统盘；
- Windows 软件；
- Windows 数据盘。

支持：

- 日志；
- 权限；
- 大文件；
- 链接；
- 压缩和加密等高级功能。

macOS 默认通常只能读取 NTFS，不能像 Windows 一样完整原生写入。

## 11.2 exFAT

适合 Windows、macOS 和 Linux 之间交换普通文件。

适合保存：

- 视频；
- 图片；
- PDF；
- 压缩包；
- 飞行日志；
- 数据集文件；
- Office 文档。

不适合直接保存并运行：

- Ubuntu 根目录；
- Docker 数据目录；
- Python 虚拟环境；
- ROS 2 工作空间；
- 依赖 Linux 权限和符号链接的项目。

exFAT 缺少 NTFS、ext4 那样完整的日志和权限能力，突然拔盘时风险更高。

## 11.3 ext4

Linux 常用文件系统。

适合：

- Ubuntu；
- ROS 2；
- Docker；
- Linux 用户目录；
- Linux 开发环境。

支持：

- Linux 权限；
- 符号链接；
- 日志；
- inode；
- 扩展属性。

Windows 和 macOS 默认不能完整读写 ext4。

## 11.4 APFS

现代 macOS 常用文件系统。

适合：

- macOS 系统；
- Mac 专用外置 SSD；
- Time Machine；
- 加密卷和快照。

Windows 默认不能读取 APFS。

## 11.5 FAT32

兼容性很广，常用于：

- EFI 系统分区；
- 老设备；
- 小容量启动介质。

缺点是单个文件通常不能超过 4GB。

## 11.6 文件系统选择表

| 用途 | 推荐文件系统 |
|---|---|
| Windows 系统盘 | NTFS |
| Ubuntu 系统盘 | ext4 |
| macOS 系统盘 | APFS |
| Windows 与 Mac 共享普通文件 | exFAT |
| EFI 启动分区 | FAT32 |

---

# 12. Ubuntu 安装盘、启动盘和系统盘

## 12.1 安装盘

将 Ubuntu ISO 写入 U 盘后，这个 U 盘用于：

- 启动安装程序；
- 试用 Ubuntu；
- 修复系统。

它不一定是长期使用的 Ubuntu 系统。

## 12.2 系统盘

Ubuntu 真正安装所在的 SSD 或 HDD。

其中包含：

- Linux 内核；
- 软件；
- 用户目录；
- 驱动；
- 配置；
- 日志。

## 12.3 启动盘

包含可被 UEFI 识别的启动文件。

一块外置 NVMe SSD 可以同时是：

- Ubuntu 系统盘；
- Ubuntu 启动盘。

---

# 13. 外置 NVMe 安装 Ubuntu

## 13.1 为什么 NVMe 外置盘适合 Ubuntu

相比外置机械硬盘：

- 随机读写快很多；
- 开机速度快；
- ROS 2 编译更顺畅；
- Docker 镜像和容器运行更合适；
- 抗震性更好；
- 可以以后拆出装进电脑内部。

## 13.2 外置系统的额外风险

外置 Ubuntu 还依赖：

- USB 接口；
- 数据线；
- 硬盘盒；
- 桥接芯片；
- USB 供电；
- 睡眠唤醒机制。

运行 Ubuntu 时连接突然断开，相当于：

```text
电脑运行时直接拔掉系统盘
```

可能造成：

- 系统卡死；
- 文件损坏；
- ext4 需要修复；
- Docker 数据损坏；
- Ubuntu 无法启动。

## 13.3 外置 Ubuntu 使用原则

- 使用原装或高质量短线；
- 直接连接笔记本接口；
- 不经过廉价扩展坞；
- 运行时固定放置；
- 不要边运行边移动；
- 不要随意拔出；
- 定期备份系统和项目。

## 13.4 推荐分区方案

### 方案 A：整块盘专门用于 Ubuntu

| 分区 | 建议大小 | 文件系统 | 挂载点 |
|---|---:|---|---|
| EFI | 512MB～1GB | FAT32 | `/boot/efi` |
| Ubuntu | 剩余空间 | ext4 | `/` |

交换空间可以使用 Ubuntu 默认的 swapfile。

### 方案 B：Ubuntu + 跨平台共享区

| 分区 | 建议大小 | 文件系统 | 用途 |
|---|---:|---|---|
| EFI | 512MB～1GB | FAT32 | Ubuntu 启动 |
| Ubuntu | 300GB 或更多 | ext4 | 系统和开发 |
| 共享区 | 剩余空间 | exFAT | Windows、Mac、Ubuntu 共享普通文件 |

不要把 ROS 工作空间、Docker 数据和 Python 虚拟环境直接放在 exFAT 中。

## 13.5 GRUB 和 EFI 的位置

外置安装时，理想状态是：

- Ubuntu 系统在外置 SSD；
- EFI 分区在外置 SSD；
- GRUB 也安装到外置 SSD；
- Windows 内置盘保持独立。

这样：

```text
插上外置 SSD → 可以选择 Ubuntu
拔掉外置 SSD → Windows 独立启动
```

---

# 14. 硬盘为什么会损坏

## 14.1 HDD 常见故障

- 通电时跌落；
- 磁头损坏；
- 盘片划伤；
- 电机故障；
- 坏扇区增加；
- USB 接口损坏；
- 电路板故障；
- 文件系统损坏。

## 14.2 SSD 常见故障

- 主控损坏；
- 固件异常；
- NAND 老化；
- 映射表损坏；
- 过热；
- 供电不稳定；
- USB 桥接芯片故障；
- 文件系统损坏。

## 14.3 HDD 与 SSD 的风险差异

| 风险 | HDD | SSD |
|---|---|---|
| 通电跌落 | 非常危险 | 相对抗震 |
| 机械磨损 | 有 | 无 |
| 主控故障 | 可能 | 可能 |
| 固件故障 | 可能 | 可能 |
| 冷备成本 | 低 | 高 |
| 随身运行系统 | 不理想 | 合适 |
| 数据恢复 | 某些故障相对可行 | 主控故障时较困难 |

简单记忆：

```text
HDD 怕摔、怕动、怕机械老化
SSD 怕主控、固件、过热、供电和突然失效
```

SSD 抗摔不代表数据绝对安全。

## 14.4 逻辑损坏与物理损坏

### 逻辑损坏

硬件未必损坏，但数据结构出现问题，例如：

- 突然拔盘；
- 文件系统损坏；
- 分区表误删；
- 误格式化；
- 勒索软件加密。

### 物理损坏

硬件本身出现故障，例如：

- HDD 盘片划伤；
- 磁头损坏；
- SSD 主控损坏；
- NAND 失效；
- USB 接口脱落。

---

# 15. SMART、检测和验盘

SMART 是硬盘自我监测信息。

## 15.1 HDD 可能显示

- 通电时间；
- 温度；
- 重映射扇区；
- 待处理扇区；
- 读取错误；
- 启停次数。

## 15.2 SSD 可能显示

- 总写入量；
- 温度；
- 剩余寿命；
- 不安全关机次数；
- 介质错误；
- 错误记录。

## 15.3 Windows 检测工具

可使用：

- CrystalDiskInfo：查看型号、健康和温度；
- CrystalDiskMark：简单测试速度；
- 磁盘管理：查看物理磁盘和分区；
- 任务管理器：查看磁盘活动。

PowerShell 查看物理磁盘：

```powershell
Get-PhysicalDisk |
    Select-Object FriendlyName, MediaType, Size, BusType |
    Format-Table -AutoSize
```

查看磁盘设备：

```powershell
Get-CimInstance Win32_DiskDrive |
    Select-Object Model, Size, InterfaceType |
    Format-Table -AutoSize
```

## 15.4 Linux 检测

```bash
sudo apt update
sudo apt install smartmontools
sudo smartctl -a /dev/sdX
```

NVMe 设备可能是：

```bash
sudo smartctl -a /dev/nvme0
```

注意：

- 外置硬盘盒不一定完整透传 SMART；
- SMART 正常不代表硬盘绝对不会突然损坏。

## 15.5 新盘验收步骤

1. 核对型号和容量；
2. 查看 SMART；
3. 检查温度；
4. 测试读写速度；
5. 复制大量数据观察是否掉盘；
6. 检查数据线是否松动；
7. 确认休眠唤醒后仍能识别；
8. 确认没有异常声音或反复断连。

---

# 16. 删除、格式化与安全擦除

## 16.1 删除文件

删除通常只是告诉文件系统：

```text
这块空间以后可以重新使用
```

数据不一定立即从物理介质消失。

## 16.2 快速格式化

主要重建文件系统结构，一般不会逐字节覆盖整块硬盘。

## 16.3 完整格式化

进行更多检查或覆盖，耗时更长。

## 16.4 分区

分区是改变一块盘的逻辑空间划分。

```text
分区 ≠ 格式化
格式化 ≠ 删除单个文件
```

## 16.5 SSD 安全擦除

SSD 不适合简单反复覆盖。

更合理的方法包括：

- 厂商安全擦除；
- NVMe Secure Erase；
- 系统提供的安全初始化；
- 加密后销毁密钥。

---

# 17. 安全弹出、断电与文件系统损坏

操作系统写文件时，数据可能先进入 RAM 缓存，稍后才真正写入硬盘。

安全弹出会：

1. 将缓存数据写完；
2. 停止相关读写；
3. 卸载文件系统；
4. 确认设备可以拔出。

直接拔盘可能导致：

- 当前文件损坏；
- 文件夹目录损坏；
- 分区需要修复；
- exFAT 文件系统异常；
- Ubuntu 无法启动。

特别注意：

- exFAT 缺少完整日志能力；
- 外置 Ubuntu 运行时绝不能直接拔盘；
- 即使复制进度条结束，也应先安全弹出。

---

# 18. 备份、同步、归档与 RAID

## 18.1 备份

备份是保留另一份独立副本。

例如：

```text
电脑内置 SSD 有一份
移动机械硬盘再保存一份
```

## 18.2 同步

同步让两个位置保持一致。

如果误删原文件，同步工具可能也会删除另一边，因此同步不一定等于备份。

## 18.3 归档

归档用于保存已经完成、很少修改的数据，例如：

- 历史比赛项目；
- 旧数据集；
- 原始视频；
- 历史飞行日志；
- 旧版系统镜像。

## 18.4 RAID

RAID 可以提高存储系统的可用性，但不能防止：

- 误删除；
- 勒索软件；
- 文件覆盖；
- 设备被盗；
- 火灾；
- 多块盘同时损坏。

```text
RAID ≠ 备份
同步 ≠ 备份
```

## 18.5 3-2-1 原则

理想结构：

- 至少 3 份数据；
- 使用 2 种不同介质；
- 至少 1 份异地保存。

针对个人项目，可简化为：

```text
当前工作副本：电脑或 Ubuntu SSD
        +
本地备份：移动机械硬盘
        +
异地副本：GitHub、云盘或其他地点
```

---

# 19. 不同存储设备的正确用途

| 设备 | 最适合 | 不适合 |
|---|---|---|
| 内置 NVMe SSD | 系统、软件、编译、Docker | 唯一备份 |
| 外置 NVMe SSD | 移动 Ubuntu、项目、高速传输 | 唯一长期冷备 |
| SATA SSD | 系统、普通开发、旧电脑升级 | 极限高速工作 |
| 移动 HDD | 冷备、视频、数据集归档 | 高频移动时运行系统 |
| U 盘 | 临时传文件、制作安装盘 | 长期运行 Ubuntu |
| SD 卡 | 相机、无人机、嵌入式设备 | 重要数据唯一副本 |
| NAS | 多设备共享、自动备份 | 唯一异地备份 |
| 云盘 | 文档同步、异地副本 | 大量本地高速数据 |

---

# 20. 适合你的存储结构

你的典型环境：

- 华硕天选 5 Pro；
- Windows；
- Apple Silicon MacBook Air；
- Ubuntu；
- ROS 2；
- PX4；
- Gazebo；
- Docker；
- 视频、数据集和飞行日志。

推荐采用分层结构。

## 20.1 天选 5 Pro 内置 SSD

负责：

- Windows；
- Windows 软件；
- 常用项目；
- 当前 Windows 数据；
- 游戏和应用。

标准 1TB 出厂配置通常是一块 1TB NVMe SSD，占用一个 M.2 插槽；C 盘、D 盘可能只是同一块物理 SSD 的不同分区。最终应通过任务管理器或 PowerShell 确认。

## 20.2 第二块 NVMe SSD

### 优先方案：内置

如果第二个 M.2 插槽空闲，可将新增 NVMe 直接内置，专门安装 Ubuntu。

优点：

- 稳定；
- 性能高；
- 不依赖 USB 线；
- 睡眠唤醒更可靠；
- 适合 ROS 2、Docker 和 Gazebo。

### 便携方案：绿联 10Gbps NVMe 硬盘盒

适合：

- 不想拆机；
- 需要把 Ubuntu 拔下来携带；
- 希望以后将 SSD 转为内置。

外置方案应使用可靠数据线并避免运行时移动。

## 20.3 4TB 移动机械硬盘

负责：

- 无人机视频；
- 大型数据集；
- 历史项目；
- 系统镜像；
- PX4 日志；
- 安装包；
- 课程资料；
- Ubuntu 盘的备份。

## 20.4 GitHub 或云盘

负责：

- 代码；
- 文档；
- 参数文件；
- 项目说明；
- 报告；
- 小型配置；
- 异地备份。

最终结构：

```text
内置 SSD：日常系统与软件
第二块 NVMe / 外置 NVMe：Ubuntu 开发
移动 HDD：本地冷备
GitHub / 云盘：异地副本
```

---

# 21. 购买 NVMe SSD 的判断方法

对于绿联 USB 3.2 Gen 2 10Gbps NVMe 硬盘盒，SSD 峰值速度不是第一优先级。

因为硬盘盒实际速度通常不超过约 1GB/s，即使购买 7000MB/s 的高端 SSD，也无法在该硬盘盒中发挥完整性能。

## 21.1 推荐规格

- M.2 2280；
- PCIe NVMe；
- 无原装厚散热片；
- 优先 TLC；
- 正规品牌；
- 五年左右质保更理想；
- 发热和功耗适中；
- 正规自营或官方渠道。

## 21.2 不推荐

- M.2 SATA 盘误买到 NVMe-only 硬盘盒；
- PCIe 5.0 高发热旗舰盘；
- 带厚散热片的版本；
- 拆机盘；
- 工包盘；
- 清零盘；
- 二手盘；
- 来源不明的杂牌盘；
- 只宣传峰值速度但不说明具体型号的产品。

## 21.3 适合外置使用的型号思路

可优先关注：

- WD Blue SN580；
- WD Black SN770；
- Lexar NM790；
- 致态 TiPlus7100 系列；
- 三星部分主流 NVMe 型号。

具体购买时应比较：

- 同容量实际成交价；
- TLC/QLC；
- 质保；
- 是否无散热片；
- 是否全新零售版；
- 正规渠道；
- 发热与稳定性口碑。

对于 10Gbps 硬盘盒，没有必要为 PCIe 5.0 或极限顺序速度支付明显溢价。

---

# 22. 512GB、1TB 和 2TB 如何选择

## 22.1 512GB / 500GB

格式化后可用空间通常约 465～477GiB。

适合：

- Ubuntu；
- ROS 2；
- PX4；
- VS Code；
- Python 环境；
- 轻中度 Docker；
- 少量 Gazebo 资源；
- 当前项目。

不适合长期堆积：

- 大量 Docker 镜像；
- 多套 Ubuntu 环境；
- YOLO 大型数据集；
- 长期无人机视频；
- 多个虚拟机镜像。

### 大致空间消耗

| 内容 | 可能占用 |
|---|---:|
| Ubuntu 与常用软件 | 40～70GB |
| ROS 2 | 10～30GB |
| PX4、Gazebo 和构建文件 | 20～80GB |
| Python 环境 | 10～40GB |
| Docker | 50～150GB 或更多 |
| 当前项目 | 剩余空间 |

如果大型数据放到机械硬盘，512GB 可以使用。

## 22.2 1TB

适合长期开发：

- Ubuntu；
- ROS 2；
- PX4；
- Gazebo；
- Docker；
- 多个 Python 环境；
- 当前视觉数据；
- 若干大型项目。

对于大多数开发者，1TB 是容量、价格和管理难度的平衡点。

## 22.3 2TB

适合：

- 多套 ROS 或 Ubuntu 环境；
- 大量 Docker 镜像；
- 视觉数据集；
- 虚拟机；
- 视频；
- 不希望短期内扩容。

## 22.4 价格判断

不要只看总价，要看容量升级的差价。

推荐规则：

```text
512GB 比 1TB 便宜很多 → 512GB 可以买
512GB 只比 1TB 便宜很少 → 直接买 1TB
```

同理，如果 2TB 的每 GB 成本明显更低，而且预算允许，可以选择 2TB。

---

# 23. 常见误区

1. **M.2 不等于 NVMe。**
2. **USB-C 不等于高速。**
3. **SSD 不等于永远不会坏。**
4. **HDD 能装系统，但不代表适合长期运行系统。**
5. **最高顺序速度不等于系统流畅度。**
6. **硬盘盒不是单纯的金属壳。**
7. **C 盘和 D 盘不一定是两块硬盘。**
8. **分区、卷和文件系统不是同一个概念。**
9. **删除文件不等于立即彻底擦除。**
10. **格式化不等于硬盘物理损坏。**
11. **exFAT 适合共享文件，不适合 Ubuntu 根目录。**
12. **同步不等于备份。**
13. **RAID 不等于备份。**
14. **只有一份数据，就没有真正备份。**
15. **抗摔不等于数据绝对安全。**
16. **硬盘健康正常不代表明天一定不会坏。**
17. **高端 SSD 放进 10Gbps 硬盘盒不会保持 7000MB/s。**
18. **外置 Ubuntu 的最大风险往往是线材和断连，而不是 SSD 峰值性能。**

---

# 24. 最终检查清单

## 24.1 分析一个存储设备

按顺序问：

```text
1. 它使用什么存储介质？
   HDD 还是 NAND 闪存？

2. 它使用什么协议？
   SATA 还是 NVMe？

3. 数据经过什么通道？
   PCIe、USB 还是 Thunderbolt？

4. 外形和接口是什么？
   2.5 英寸、M.2 2280、USB-C？

5. 电脑和硬盘盒是否兼容？
   NVMe-only 还是 SATA/NVMe 双协议？

6. 使用什么分区表？
   GPT 还是 MBR？

7. 使用什么文件系统？
   NTFS、exFAT、ext4、APFS 还是 FAT32？

8. 它承担什么角色？
   系统盘、工作盘、共享盘还是冷备盘？

9. 数据损坏后是否还有另一份？
```

## 24.2 购买 NVMe SSD 前

- [ ] 确认硬盘盒支持 NVMe；
- [ ] 确认 SSD 为 M.2 2280；
- [ ] 确认不是 M.2 SATA；
- [ ] 购买无散热片版本；
- [ ] 确认容量；
- [ ] 优先正规零售版；
- [ ] 避免拆机、工包、清零和二手盘；
- [ ] 比较 512GB 与 1TB 的实际差价；
- [ ] 不为硬盘盒无法发挥的峰值速度支付过多溢价。

## 24.3 收到 SSD 后

- [ ] 检查包装和防拆；
- [ ] 正确安装导热垫；
- [ ] 使用原装数据线；
- [ ] 查看型号和容量；
- [ ] 查看 SMART；
- [ ] 检查温度；
- [ ] 测试读写速度；
- [ ] 连续复制大文件检查掉盘；
- [ ] 测试休眠唤醒；
- [ ] 确认稳定后再安装 Ubuntu。

## 24.4 日常使用

- [ ] 外置系统运行时不移动硬盘盒；
- [ ] 不通过廉价扩展坞连接；
- [ ] 不直接拔盘；
- [ ] 共享盘优先安全弹出；
- [ ] 定期检查磁盘健康；
- [ ] 定期清理 Docker 和构建缓存；
- [ ] 重要项目至少保留两份；
- [ ] 代码和文档上传 GitHub 或云端；
- [ ] 大型数据和视频备份到机械硬盘。

---

# 总结

最适合个人开发者理解存储设备的方法是将其分成四层：

```text
第一层：物理介质
HDD / NAND 闪存

第二层：连接和协议
SATA / NVMe / PCIe / USB

第三层：磁盘组织
GPT / MBR / 分区 / 卷

第四层：文件与系统
NTFS / exFAT / ext4 / APFS / EFI / GRUB
```

适合你的长期存储结构是：

```text
天选 5 Pro 内置 SSD
    → Windows 与日常软件

第二块内置 NVMe 或绿联 10Gbps 外置 NVMe
    → Ubuntu、ROS 2、PX4、Docker、Gazebo

4TB 移动机械硬盘
    → 数据集、视频、日志、历史项目和冷备

GitHub / 云盘
    → 代码、文档和异地副本
```

最终原则：

> **SSD 负责运行，HDD 负责容量，云端或另一设备负责防止单点丢失。**
>
> **性能必须与接口匹配，文件系统必须与用途匹配，重要数据必须存在多个独立副本。**
